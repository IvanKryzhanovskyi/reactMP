import React from 'react';
import { ReactComponent as ReactLogo } from './React-icon.svg';

import styles from './styles.module.scss';

export const ReactIcon = () => (
  <ReactLogo className={styles.ReactIcon} />
);
